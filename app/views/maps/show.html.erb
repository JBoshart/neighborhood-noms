<% if @new_noms[0].nil? %>
  <%= "There are no restaurants in that search! Try again with another parameter!" %>
<% else %>
  <% @new_noms[0].each do |nom| %>
    <br /><%= nom.name %>
    <br /><%= nom.address %>
    <br />
  <% end %>
<% end %>
